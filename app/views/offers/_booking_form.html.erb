<div class="">
  <div class="row">
    <div class="">
      <div class="bg-white p-3 shadow" style="border-radius: 10px">
        <div class ="border-radius">
          <h3> <strong>Book a New Offer </strong></h3>
          <%= simple_form_for [@offer, @booking], data: { controller: 'dater', dater_price_value: @offer.price } do |f| %>
            <strong><%= "#{@offer.price}""€ "%></strong><%="per day"%>
            <%= f.input :start_date, input_html: {data: { controller: 'datepicker', dater_target: "startDate",  } }, as: :string, style: "border:none border-radius: 25px" %>
            <%= f.input :end_date, input_html: {data: { controller: 'datepicker', action:"change->dater#count",  dater_target: "endDate"} }, as: :string %>
            <%# <%= start_date - end_date  %>
            <%= f.input :comment, placeholder: 'comment' %>
            <%# <%= f.input :total_amount, {data: { dater_target: "totalAmount", dater_price_value: @offer.price } }  %>
            <hr>
            <%= p (f.input :total_amount, disabled: true, readonly: true, input_html: { data: { dater_target: "totalAmount" } }) %>
            <%= f.submit class: 'btn btn-success color-C23028' %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
<%# data_dater_price_value: @offer.price %>
<%# datepicker_target: "startTime"
 datepicker_target: "endTime" }, id:'end_time'  %>
